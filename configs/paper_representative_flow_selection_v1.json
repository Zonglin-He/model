{
  "protocol": "paper_representative_flow_selection_v1",
  "status": "pre_registered_before_completion_of_the_formal_full_no_ssaw_run",
  "datasets": [
    "EEG",
    "HAR",
    "HHAR"
  ],
  "source_seeds": [
    1,
    2,
    3
  ],
  "input_variant": "full",
  "selection_uses_target_labels": false,
  "selection_uses_f1": false,
  "primary_score": "mean_over_batches_and_source_seeds(ssaw_training_participation_rate * min(ssaw_weighted_consistency_loss / max(raw_ce_loss, 1e-12), 1.0))",
  "selection_reason": "Eligible coverage is almost saturated in the current implementation, so realized label-free auxiliary signal density is more discriminative than coverage alone.",
  "tie_breakers": [
    "mean_over_batches_and_source_seeds(ssaw_admitted_participation_rate)",
    "lexicographic_scenario"
  ],
  "active_batch_rule": {
    "metric": "ssaw_admitted_participation_rate",
    "minimum": 0.25
  },
  "future_horizon_batches": 5,
  "reporting_scope": "descriptive_target_selected_evaluation",
  "notes": [
    "The selected flow is used only for mechanism, safety, and efficiency panels.",
    "All five flows and all three source seeds remain in the formal main table.",
    "Overall and active-batch effects must be reported together."
  ]
}
